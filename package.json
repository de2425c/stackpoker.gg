{
  "name": "stackpoker-landing",
  "version": "1.0.0",
  "description": "StackPoker.gg landing page with Python backend",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "install-python": "cd parser_backend && pip install -r requirements.txt",
    "start-all": "NODE_ENV=development concurrently \"npm run dev\" \"cd parser_backend && uvicorn main:app --reload --host 0.0.0.0 --port 8000\""
  },
  "engines": {
    "node": "18.x"
  },
  "dependencies": {
    "express": "^4.18.2",
    "http-proxy-middleware": "^2.0.6"
  },
  "devDependencies": {
    "concurrently": "^8.2.0",
    "nodemon": "^3.0.1"
  }
} 